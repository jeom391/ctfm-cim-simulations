[build-system]
requires = ["setuptools>=77,<83"]
build-backend = "setuptools.build_meta"

[project]
name = "ctfm-worker"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = ["ctfm-api==0.1.0", "matplotlib>=3.9,<4"]

[tool.setuptools.packages.find]
where = ["src"]
